pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'mkdir /opt/test1'
            }
        }
        stage('abhishek') {
            steps {
                sh 'mkdir /opt/test2'
            }
        }
    }
}
